SchemaVersion: "1.0"
Actions:
  Build_6f:
    Identifier: aws/build@v1
    Inputs:
      Sources:
        - WorkflowSource
    Outputs:
      Reports:
        SAMPLE_REPORT:
          IncludePaths:
            - report.xml
          Format: JunitXml
    Configuration:
      Steps:
        - Run: echo "Hello, World!"
        - Run: wget $UA_DOWNLOAD_LINK -O $UA_JAR
        - Run: java -jar $UA_JAR -licenseKey "${MEND_LICENSE_KEY}" -generateSarifReport true -project sarif-report -product sarif-support